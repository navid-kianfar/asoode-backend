docker build \
    -t asoode-servers-{APP_LOWERED}-{ENV_LOWERED}:{VERSION} . \
    -f ./Dockerfile-{APP_LOWERED} \
    --build-arg APP_BUILD={VERSION} \
    --build-arg APP_ENVIRONMENT={ENV} \
    --build-arg APP_I18N_LANG={APP_I18N_LANG} \
    --build-arg APP_I18N_CULTURE={APP_I18N_CULTURE} \
    --build-arg APP_AUTH_ISSUER={APP_AUTH_ISSUER} \
    --build-arg APP_AUTH_SECRET={APP_AUTH_SECRET} \
    --build-arg APP_AUTH_SALT={APP_AUTH_SALT} \
    --build-arg APP_DB_SERVER={APP_DB_SERVER} \
    --build-arg APP_DB_PORT={APP_DB_PORT} \
    --build-arg APP_DB_USER={APP_DB_USER} \
    --build-arg APP_DB_PASS={APP_DB_PASS} \
    --build-arg APP_STORAGE_SERVER={APP_STORAGE_SERVER} \
    --build-arg APP_STORAGE_PORT={APP_STORAGE_PORT} \
    --build-arg APP_STORAGE_USER={APP_STORAGE_USER} \
    --build-arg APP_STORAGE_PASS={APP_STORAGE_PASS} \
    --build-arg APP_STORAGE_BUCKET={APP_STORAGE_BUCKET} \
    --build-arg APP_QUEUE_SERVER={APP_QUEUE_SERVER} \
    --build-arg APP_QUEUE_PORT={APP_QUEUE_PORT} \
    --build-arg APP_QUEUE_USER={APP_QUEUE_USER} \
    --build-arg APP_QUEUE_PASS={APP_QUEUE_PASS} \
    --build-arg APP_QUEUE_PREFIX={APP_QUEUE_PREFIX} \
    --build-arg APP_SMTP_SERVER={APP_SMTP_SERVER} \
    --build-arg APP_SMTP_PORT={APP_SMTP_PORT} \
    --build-arg APP_SMTP_USER={APP_SMTP_USER} \
    --build-arg APP_SMTP_PASS={APP_SMTP_PASS} \
    --build-arg APP_SMTP_SSL={APP_SMTP_SSL} \